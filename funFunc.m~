function [selectedFea] = funFunc(XTRAIN, ytrain, XTEST, ytest)
    k = 0;
    D = 0;
    for i=1:21 
        if (k <= 8)
            dX = (pdist(XTRAIN).^2);
            dY = (pdist(ytrain).^2)
            candidate = max(dX);
            k = k +1; 
            D = D + candidate;
        end
        
    end
    dY = squareform((pdist(ytrain)));
    selectedFea = 
end
